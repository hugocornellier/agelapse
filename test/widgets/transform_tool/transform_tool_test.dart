import 'package:flutter_test/flutter_test.dart';
import 'package:agelapse/widgets/transform_tool/transform_tool.dart';

/// Unit tests for TransformTool widget.
void main() {
  group('TransformTool', () {
    test('class can be referenced', () {
      expect(TransformTool, isNotNull);
    });
  });
}
